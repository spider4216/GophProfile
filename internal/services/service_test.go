package services

func prepareService(store map[string][]string) (*Service, error) {
	// logger := logger.Init("debug")
	// rep := reptest.NewRepository(logger, store)

	// return New(
	// 	WithHTTPClient(cli),
	// 	WithHost(""),
	// 	WithRepo(rep),
	// 	WithLogger(logger),
	// )

	return nil, nil
}
